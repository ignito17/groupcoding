#include <stdio.h>
int main()
{
	int A,B,C;
	printf("please enter the two value simultaneously\n");	    
	scanf("%d%d", &A, &B);
	C==B;
		printf("the numbers u entered are\nA=%d B=%d\n",A, B);	
		int a;
		printf("type \'0'\ and press enter to swap nos\n");
		scanf ("%d", &a);
		if (a==0) 
		{
				printf("the swapped numbers are A=%d B=%d", B, A);
		} 		
		
		
}